spring:
  application:
    name: apims-cps
  profiles:
    active: prod
  cloud:
    config:
      discovery:
        enabled: true
        service-id: apims-config-server
eureka:
  client:
    service-url:
      defaultZone: http://apims:9900/eureka
